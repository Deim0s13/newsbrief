apiVersion: v1
kind: Service
metadata:
  name: newsbrief
  labels:
    app.kubernetes.io/name: newsbrief
    app.kubernetes.io/component: api
  annotations:
    argocd.argoproj.io/sync-wave: "1"  # Create with deployment
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: newsbrief
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 8787
      targetPort: http
      protocol: TCP
